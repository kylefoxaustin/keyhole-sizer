# keyhole-sizer — interactive NPU sizing sandbox for the Keyhole bake-off findings
# (retrofitted onto ratchet v0.2.4 — phase 3 of the engine consolidation)

# Shared SoC sizing engine — canonical tiers, anchor loader, capability tables.
# Installed from the public GitHub source repo (not PyPI — the name "ratchet"
# on PyPI is an unrelated package). Pin to a git tag so deploys are
# reproducible; bump this line when adopting a new ratchet release.
#
# 2026-07-26: v0.2.7 -> v0.3.2. NOT a routine bump — measured first, because the
# old ceiling was a doc claim nobody had tested. A 3,714-cell A/B matrix (every
# tier x variant x pipeline x resolution, every LLM quant x workload x precision
# set x FP4-maturity, per-model alias paths, all 6 VLA models, capability badges
# and tier specs) was built under BOTH engines and diffed: 3714/3714 IDENTICAL,
# 0 cells raised either side. v0.3.x is additive for this surface (v0.3.0 =
# CPU-complex + perception modeling we don't import; v0.3.1 = an i.MX 93 clock
# correction; v0.3.2 = two new CalibrationMethod values). Remote tag verified to
# dereference to the exact commit tested (v0.3.2^{} = 7ff256c) — check the ^{}
# row of `git ls-remote --tags`, not the tag-object SHA above it.
ratchet @ git+https://github.com/kylefoxaustin/ratchet.git@v0.3.2

streamlit>=1.40.0
pandas>=2.2.0
numpy>=1.26.0
plotly>=5.24.0
# XLSX export with formatting (used by sizer/kpi_breakdown.py for the
# KPI-breakdown download button — bold/alignment can't go through CSV).
openpyxl>=3.1.0

# 2026-05-29: rebuild trigger — force a full Streamlit Cloud restart to pick up
# the VLA arc (Phases 3-5, sizer/*.py: vla_models, project_vla). A requirements
# change forces an env reinstall = full process restart, clearing the stale
# sys.modules cache the same way a dashboard reboot does. Needed because the
# dashboard reboot was blocked by a GitHub OAuth 500. No dependency changes.
# 2026-05-30: re-bumped — the multi-camera UI (app.py) calls the Phase 3c engine
# (n_cameras/fleet_size/camera_mode in project_vla, landed in b9a9293+03986a8
# AFTER the last full restart). app.py hot-reloads but those sizer/*.py changes
# would stay cached → TypeError on the live app. This bump forces the restart so
# the 3c engine + multi-cam UI load atomically. No dependency changes.
# 2026-05-30 (2): re-bumped — added camera-FPS + DDR-bandwidth-demand metrics to
# project_vla (_add_throughput_metrics, sizer/*.py). Force a full restart so the
# new engine fields load and the UI's perception/BW caption appears. No dep changes.
# 2026-06-05: redeploy trigger — FP4 NPU precision-set selector (43c5e20, ratchet
# ADR 017). hw_with_precision + _precision_prefill_scale + the project_llm
# fp4_runtime_maturity param live in sizer/npu_model.py; app.py hot-reload alone
# leaves that engine module cached. Force a full env reinstall so the escalating
# radio + 3-col compare load with the new engine. No dependency changes.
# 2026-06-05 (2): redeploy trigger — UX pass (app.py only, no engine change):
# precision selector relocated out of the sidebar into a self-contained "precision
# what-if" compare in the LLM section (headline now sizes the real stock tier);
# VLA units legend + always-filled perception slot + clearer deferred/won't-run
# messaging. Bump forces the Cloud restart so the new app.py loads. No dep changes.
# 2026-06-06: redeploy trigger — VLA headline now MIRRORS the vision pipeline's
# 4-metric format (per-camera FPS / aggregate FPS / memory fit / DDR-BW-ratio-vs-
# Mid), stable across every model. The topology-dependent third tile (Hz /
# actions / ms-per-tok that swapped units per model) + control-loop Hz move to a
# de-emphasized caption + expander — Hz is confusing to a vision reviewer.
# app.py only, no engine change. Bump forces the Cloud restart. No dep changes.
